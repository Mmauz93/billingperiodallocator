// Import the custom not-found component
import CustomNotFoundPage from './_not-found/page';

// Reuse our custom not-found implementation
export default function NotFound() {
  return <CustomNotFoundPage />;
} 
